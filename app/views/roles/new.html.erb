<!-- Header Section -->
<div class="bg-white border-b border-gray-200">
    <div class="px-4 sm:px-6 lg:px-8 py-6">
        <div class="flex items-center justify-between">
            <div class="flex items-center space-x-3">
                <div class="flex items-center justify-center w-10 h-10 bg-blue-100 rounded-lg">
                    <i class="fas fa-user-shield text-blue-600 text-lg"></i>
                </div>
                <div>
                    <h1 class="text-2xl font-bold text-gray-900">Crear Nuevo Rol</h1>
                    <p class="text-sm text-gray-500 mt-1">Define un nuevo rol con permisos específicos para el sistema</p>
                </div>
            </div>
            <%= link_to show_roles_path, 
                class: "inline-flex items-center px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 hover:text-gray-900 transition-colors duration-200" do %>
                <i class="fas fa-arrow-left mr-2"></i>
                Volver
            <% end %>
        </div>
    </div>
</div>

<!-- Main Content -->
<div class="px-4 sm:px-6 lg:px-8 py-8">
    <%= form_for @rol, url: create_roles_path, local: true do |f| %>
        <!-- Error Messages -->
        <% if @rol.errors.any? %>
            <div class="mb-6 bg-red-50 border border-red-200 rounded-lg p-4">
                <div class="flex items-start">
                    <i class="fas fa-exclamation-triangle text-red-400 text-lg mt-0.5 mr-3"></i>
                    <div class="flex-1">
                        <h3 class="text-sm font-medium text-red-800">
                            <%= pluralize(@rol.errors.count, "error") %> impidieron que el rol se guardara:
                        </h3>
                        <ul class="mt-2 text-sm text-red-700 list-disc list-inside">
                            <% @rol.errors.full_messages.each do |message| %>
                                <li><%= message %></li>
                            <% end %>
                        </ul>
                    </div>
                </div>
            </div>
        <% end %>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- Role Information Card -->
            <div class="lg:col-span-1">
                <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
                    <div class="flex items-center space-x-3 mb-6">
                        <div class="flex items-center justify-center w-8 h-8 bg-blue-100 rounded-lg">
                            <i class="fas fa-info-circle text-blue-600"></i>
                        </div>
                        <h2 class="text-lg font-semibold text-gray-900">Información del Rol</h2>
                    </div>
                    
                    <div class="space-y-4">
                        <%= f.label :name, "Nombre del Rol", class: "block text-sm font-medium text-gray-700" %>
                        <%= f.text_field :name, 
                            class: "mt-1 block w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200",
                            placeholder: "Ej: Administrador, Vendedor, etc.",
                            oninput: "this.value = this.value.toUpperCase()" %>
                    </div>

                    <!-- Role Summary Info -->
                    <div class="mt-6 p-4 bg-gray-50 rounded-lg">
                        <h4 class="text-sm font-medium text-gray-700 mb-2">Información</h4>
                        <ul class="text-xs text-gray-600 space-y-1">
                            <li>• Asigna permisos específicos por área</li>
                            <li>• Los roles pueden editarse posteriormente</li>
                            <li>• Un usuario puede tener múltiples roles</li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- Permissions Card -->
            <div class="lg:col-span-2">
                <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
                    <div class="flex items-center space-x-3 mb-6">
                        <div class="flex items-center justify-center w-8 h-8 bg-green-100 rounded-lg">
                            <i class="fas fa-lock text-green-600"></i>
                        </div>
                        <h2 class="text-lg font-semibold text-gray-900">Permisos por Área</h2>
                    </div>

                    <div class="space-y-6">
                        <% Area.all.each_with_index do |area, index| %>
                            <div class="border border-gray-200 rounded-lg p-4 hover:border-gray-300 transition-colors duration-200">
                                <div class="flex items-center justify-between mb-4">
                                    <div class="flex items-center space-x-3">
                                        <div class="flex items-center justify-center w-8 h-8 bg-indigo-100 rounded-lg">
                                            <i class="fas fa-layer-group text-indigo-600 text-sm"></i>
                                        </div>
                                        <h3 class="font-semibold text-gray-900 text-sm uppercase tracking-wide"><%= area.name %></h3>
                                    </div>
                                    <button type="button" 
                                            onclick="toggleAllPermissions('<%= area.id %>')" 
                                            class="text-xs text-blue-600 hover:text-blue-800 font-medium">
                                        Seleccionar todo
                                    </button>
                                </div>

                                <div class="grid grid-cols-2 md:grid-cols-4 gap-3">
                                    <% Permission.all.each do |perm| %>
                                        <label class="flex items-center space-x-2 p-2 rounded-lg hover:bg-gray-50 cursor-pointer transition-colors duration-200 group">
                                            <%= check_box_tag "permissions[]", 
                                                "#{area.id}-#{perm.id}", 
                                                false,
                                                class: "w-4 h-4 text-blue-600 bg-gray-100 border-gray-300 rounded focus:ring-blue-500 focus:ring-2",
                                                data: { area: area.id } %>
                                            <span class="text-sm text-gray-700 group-hover:text-gray-900 font-medium">
                                                <%= perm.name %>
                                            </span>
                                        </label>
                                    <% end %>
                                </div>
                            </div>
                        <% end %>
                    </div>
                </div>
            </div>
        </div>

        <!-- Action Buttons -->
        <div class="mt-8 flex items-center justify-end space-x-4">
            <%= link_to show_roles_path, 
                class: "inline-flex items-center px-6 py-2.5 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 hover:text-gray-900 transition-colors duration-200" do %>
                <i class="fas fa-times mr-2"></i>
                Cancelar
            <% end %>
            <%= f.submit "Crear Rol", 
                class: "inline-flex items-center px-6 py-2.5 text-sm font-medium text-white bg-blue-600 border border-transparent rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-colors duration-200" %>
        </div>
    <% end %>
</div>

<!-- JavaScript for enhanced functionality -->
<script>
function toggleAllPermissions(areaId) {
    const checkboxes = document.querySelectorAll(`input[data-area="${areaId}"]`);
    const allChecked = Array.from(checkboxes).every(cb => cb.checked);
    
    checkboxes.forEach(checkbox => {
        checkbox.checked = !allChecked;
    });
}

// Add visual feedback for checkbox changes
document.addEventListener('DOMContentLoaded', function() {
    const checkboxes = document.querySelectorAll('input[type="checkbox"]');
    checkboxes.forEach(checkbox => {
        checkbox.addEventListener('change', function() {
            const label = this.closest('label');
            if (this.checked) {
                label.classList.add('bg-blue-50', 'border-blue-200');
            } else {
                label.classList.remove('bg-blue-50', 'border-blue-200');
            }
        });
    });
});
</script>